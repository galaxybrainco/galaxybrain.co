{
  "name": "galaxybrain.co",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/galaxybrainco/galaxybrain.co.git"
  },
  "scripts": {
    "dev": "parcel dev *.html",
    "build": "parcel build *.html && cp src/CNAME dist/CNAME",
    "deploy": "npm run -s build && cd dist && git add --all && git commit -m \"Build output as of $(git log '--format=format:%H' master -1))\" && git push origin gh-pages --force && cd ../",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Galaxy Brain",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/galaxybrainco/galaxybrain.co/issues"
  },
  "homepage": "https://github.com/galaxybrainco/galaxybrain.co#readme",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "prettier": "1.18.2",
    "sass": "^1.23.0"
  }
}
